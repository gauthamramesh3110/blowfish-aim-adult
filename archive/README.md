# Archive

Superseded material. Nothing here is read by the code or referenced by the docs.

| item | what it is |
|---|---|
| `sweep-bounded.json`, `sweep-bounded.log` | A 50-run sweep from an earlier version of the mechanism that used a different neighbour relation. Kept only so the compute is not lost; the numbers are not comparable to the current results and should not be quoted. |

The live experiment writes `experiment/results/sweep.json` and is scored by
`experiment/evaluation/analyze.py`.
